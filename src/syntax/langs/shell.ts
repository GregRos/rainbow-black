import { tmMap } from "../../baker/parts.js"
import * as c from "../../colors.js"
export default tmMap
    .set(["keyword.operator.redirect"], [c.active_2, "bold"])
    .set(["source.shell constant.other.option"], c.property_1)
    .set(["support.function.command"], c.active_2)
    .set(["meta.shebang.shell"], [c.medium_object, "underline"])
    .set(["entity.name.command.shell"], c.active_2)
